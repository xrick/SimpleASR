end_point creates a feature file for each sound file containing 
		the features without silence. the file is in the 
		same directory of the sound files

Print_ph_feature creates a number of files equal to the number of phonemes
			each file contains the features . It is used by training

Print_file_feature does the same work of end_point without removing silences